flask>=3.0
anthropic>=0.57
python-dotenv>=1.0
jinja2>=3.1
markupsafe>=2.0
pandas>=2.0
openpyxl>=3.1
xlrd>=2.0
pdfkit>=1.0
pdfplumber>=0.10
# cryptography 49 has no wheel for Apple's Python 3.9 on this Mac and falls
# back to a source build that requires OpenSSL/pkg-config. Version 48 ships a
# compatible wheel and satisfies pdfminer.six's cryptography>=36 requirement.
cryptography==48.0.1
python-docx>=1.1
requests>=2.32
# System dependency: wkhtmltopdf
#   Ubuntu/Debian: apt install wkhtmltopdf
#   macOS:         brew install wkhtmltopdf
#
# Alternative PDF backend (better typography, more setup):
#   pip install weasyprint
